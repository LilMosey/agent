<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kb.infrastructure.persistence.mapper.ConversationRetrievalReferenceMapper">
    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO conversation_retrieval_reference (
            conversation_retrieval_id,
            knowledge_base_id,
            file_id,
            chunk_id,
            chunk_index,
            title_path,
            score,
            content_preview,
            created_at,
            updated_at
        )
        VALUES
        <foreach collection="entities" item="item" separator=",">
            (
                #{item.conversationRetrievalId},
                #{item.knowledgeBaseId},
                #{item.fileId},
                #{item.chunkId},
                #{item.chunkIndex},
                #{item.titlePath},
                #{item.score},
                #{item.contentPreview},
                #{item.createdAt},
                #{item.updatedAt}
            )
        </foreach>
    </insert>
</mapper>
